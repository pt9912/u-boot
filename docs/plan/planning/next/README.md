# docs/plan/planning/next

Für die nächste Iteration eingeplante Slice- und Tranchen-Pläne. Inhalte
sind ausgereift genug, um in `in-progress/` übernommen zu werden.

Übergang nach `in-progress/` per `git mv`, sobald die Arbeit beginnt.
Siehe `docs/plan/planning/README.md` für Lifecycle und
Dateiname-Konventionen.

## Bestand

| Datei | Gegenstand |
| ----- | ---------- |
| [`slice-v1-cli-json-envelope-consolidation.md`](slice-v1-cli-json-envelope-consolidation.md) | Konsolidierung CLI-JSON-Envelope-Pattern add/init/generate (R15-Cross-Slice-1). Plan User-reviewed (SD-A(a) + 2 Schutzplanken); bereit für `in-progress/` + T1. |

(Der JSON-CLI-Cluster `slice-v1-cli-json-dry-run` ist am 2026-06-08
vollständig nach `done/` ausgeliefert — 9/9 Folge-Slices + T_close.)
