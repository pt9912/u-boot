# Generated by `u-boot init` for {{.Name}}. Add project-specific
# entries below the managed block; u-boot leaves them untouched
# (LH-SA-FILE-002).

# --- BEGIN U-BOOT MANAGED BLOCK ---
# Local environment overrides; never commit.
.env
.env.local

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# OS junk
.DS_Store
Thumbs.db

# Backup files written by u-boot's overwrite-protection
# (LH-FA-INIT-005).
*.bak
*.bak.*
# --- END U-BOOT MANAGED BLOCK ---
